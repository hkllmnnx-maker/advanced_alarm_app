<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ============================================================ -->
    <!-- Permissions required by the ringing experience (Agent-06).   -->
    <!-- ============================================================ -->

    <!-- Allow vibration patterns while the alarm is firing. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Keep the CPU + screen awake so the user is actually woken up. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Allow displaying the ringing UI as a full-screen intent over the
         lock screen (Android 10+). -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Wake the device + show on top of the keyguard. -->
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.TURN_SCREEN_ON" />
    <uses-permission android:name="android.permission.SHOW_WHEN_LOCKED" />

    <!-- Receive BOOT_COMPLETED so the alarm engine (Agent-04) can
         reschedule alarms after a reboot. Declared here because the
         ringing layer is what those alarms eventually trigger. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Foreground service permission for the alarm engine. Declared
         here so it is in place by the time Agent-04 ships its service. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:label="advanced_alarm_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:showWhenLocked="true"
            android:turnScreenOn="true">

            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Full-screen-intent entry point used by the alarm engine.
                 The scheduler posts a high-priority notification whose
                 `fullScreenIntent` targets this same activity with this
                 action, and the Dart side reads the action to know it
                 should immediately push `RingingScreen` instead of the
                 normal home. -->
            <intent-filter>
                <action android:name="com.advanced_alarm_app.action.RING" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
