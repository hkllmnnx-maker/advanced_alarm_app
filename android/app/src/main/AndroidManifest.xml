<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- =================================================================== -->
    <!-- Permissions required by the alarm scheduling engine + ringing UX    -->
    <!-- =================================================================== -->

    <!-- Posting notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Vibrate when an alarm fires -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Wake the device when an alarm fires -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Required by flutter_local_notifications to deliver scheduled
         notifications at the *exact* requested time (Android 12 / API 31+).
         SCHEDULE_EXACT_ALARM is the user-revocable variant – we still ask
         for USE_EXACT_ALARM as a normal install-time alternative because
         this app is a primary alarm clock. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <!-- Required by android_alarm_manager_plus on older OEM builds, and so
         the alarm engine can reschedule alarms after a reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Foreground service that keeps the alarm UI alive when fired -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>

    <!-- Full-screen intent: lets us launch the alarm screen over the lock
         screen on Android 10+ / 14+ -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>

    <!-- Wake the device + show on top of the keyguard. -->
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.TURN_SCREEN_ON" />
    <uses-permission android:name="android.permission.SHOW_WHEN_LOCKED" />

    <!-- Request the user to whitelist us from battery optimization so that
         alarms still fire while the device is in Doze mode -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- Permission to read audio files for custom alarm sounds (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>

    <application
        android:label="advanced_alarm_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Full-screen-intent entry point used by the alarm engine.
                 The scheduler posts a high-priority notification whose
                 `fullScreenIntent` targets this same activity with this
                 action, and the Dart side reads the action to know it
                 should immediately push `RingingScreen` instead of the
                 normal home. -->
            <intent-filter>
                <action android:name="com.advanced_alarm_app.action.RING" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- =============================================================== -->
        <!-- flutter_local_notifications: receivers for scheduled / boot     -->
        <!-- redelivery of notifications.                                    -->
        <!-- =============================================================== -->

        <!-- Fires when a scheduled local notification is due. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />

        <!-- Re-schedules pending notifications after the device reboots
             (handled by flutter_local_notifications internally). -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Notification action receiver (snooze / dismiss from the notification
             without opening the app). -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false" />

        <!-- =============================================================== -->
        <!-- android_alarm_manager_plus: alarm services / receivers.         -->
        <!-- These are declared automatically by the plugin's manifest merger -->
        <!-- but we redeclare RebootBroadcastReceiver explicitly so the      -->
        <!-- AlarmService API can reschedule everything after a reboot       -->
        <!-- triggered by our own AlarmService.rescheduleAll() bootstrap.    -->
        <!-- =============================================================== -->
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.RebootBroadcastReceiver"
            android:enabled="true"
            android:exported="false"
            tools:replace="android:exported">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
