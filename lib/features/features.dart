/// Barrel file for feature modules.
///
/// Each feature (alarm list, alarm editor, settings, …) will live in its own
/// sub-folder under `lib/features/` and re-export its public surface from
/// here in later branches.
library;
